# Ignora as dependências do Node.js, pois elas serão instaladas dentro do contêiner
node_modules

# Ignora o diretório de build
dist

# Ignora o diretório do Git
.git

# Ignora arquivos de configuração do ambiente local
.env
.env.local
.env.*.local

# Ignora logs de depuração do npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignora o próprio arquivo .dockerignore e o Dockerfile
.dockerignore
Dockerfile
